# CodeBuddy Code package

The runtime provider is `acp:codebuddy` and the signing key ID is `tutti-codebuddy-release-v1`. Keep all package content declarative.
Keep `icon` aligned with the cropped left mark from the official WorkBuddy lockup; it is the shared colored identity for the Provider Rail, conversation headers, Message Center, and mentions. Keep `maskIcon` transparent and mask-safe for conversation rows. Do not include the adjacent wordmark or a mutable remote reference.
